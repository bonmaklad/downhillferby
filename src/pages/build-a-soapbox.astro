---
import Layout from '../layouts/Layout.astro';
import { buildSections, resources } from '../data/build';
import BuildGuideTeaser from '../components/BuildGuideTeaser.astro';
---

<Layout title="Build a Soapbox" description="Step-by-step guidance for constructing a safe, fast kart tailored to the Drews Ave course.">
	<section class="section-pad bg-brand-red text-white">
		<div class="mx-auto max-w-5xl space-y-6 px-6">
			<p class="text-xs uppercase tracking-[0.34em] text-brand-yellow">Workshop ready</p>
			<h1 class="section-heading text-white">Build the Drews Ave racer</h1>
			<p class="max-w-3xl text-lg text-black/80">
				Whether you’re gearing up a school team or dialling in a pro build, these steps keep you within spec while squeezing out speed.
			</p>
		</div>
	</section>
	<section class="section-pad bg-white">
		<div class="mx-auto max-w-6xl space-y-8 px-6">
			<div class="grid gap-6 md:grid-cols-2">
				{buildSections.map((section) => (
					<article class="rounded-3xl border border-brand-silver/70 bg-white p-6 shadow-sm">
						<h2 class="text-xl font-display uppercase tracking-[0.28em] text-brand-ash">{section.title}</h2>
						<ul class="mt-4 space-y-3 text-sm text-ink/80">
							{section.content.map((paragraph) => (
								<li class="flex items-start gap-3">
									<span class="mt-1 h-2 w-2 rounded-full bg-brand-red"></span>
									<span>{paragraph}</span>
								</li>
							))}
						</ul>
					</article>
				))}
			</div>
			<div class="rounded-3xl border border-brand-silver/70 bg-brand-yellow/30 p-6">
				<h2 class="text-xl font-display uppercase tracking-[0.28em] text-brand-ash">Resources</h2>
				<ul class="mt-4 space-y-3 text-sm text-ink/80">
					{resources.map((resource) => (
						<li>
							<a href={resource.href} target="_blank" rel="noreferrer noopener" class="font-semibold uppercase tracking-[0.26em] text-brand-red">
								{resource.label}
							</a>
						</li>
					))}
				</ul>
			</div>
		</div>
	</section>
	<BuildGuideTeaser />
</Layout>
