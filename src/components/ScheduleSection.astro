---
import { eventInfo } from '../data/event';
---

<section class="section-pad bg-white">
	<div class="mx-auto grid max-w-6xl gap-10 px-6 lg:grid-cols-[1fr,1.2fr]">
		<div class="space-y-4">
			<p class="text-xs uppercase tracking-[0.34em] text-brand-red">Race day timeline</p>
			<h2 class="section-heading">From dawn pits to podium</h2>
			<p class="text-lg text-ink/80">
				Plan your day so you never miss a heat. Scrutineering is strict, so be ready when your number is called.
			</p>
			<a href="/health-safety" class="inline-flex items-center justify-center rounded-full border border-brand-ash/40 px-6 py-3 text-sm font-semibold uppercase tracking-[0.28em] text-brand-ash transition hover:border-brand-red hover:text-brand-red">
				Safety &amp; scrutineering
			</a>
		</div>
		<ol class="relative space-y-6 border-l border-brand-silver/60 pl-6">
			{eventInfo.schedule.map((item) => (
				<li class="relative rounded-3xl border border-brand-silver/40 bg-white p-5 shadow-lg transition hover:-translate-y-1 hover:border-brand-red/50">
					<span class="absolute -left-[14px] top-6 h-3 w-3 rounded-full bg-brand-red"></span>
					<p class="text-xs uppercase tracking-[0.3em] text-brand-red">{item.time}</p>
					<h3 class="mt-2 text-xl font-display uppercase tracking-[0.28em] text-brand-ash">{item.label}</h3>
					{item.description && (
						<p class="mt-3 text-sm leading-relaxed text-ink/80">{item.description}</p>
					)}
				</li>
			))}
		</ol>
	</div>
</section>
